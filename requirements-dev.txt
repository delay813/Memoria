# ============================================================
# 开发 / 测试依赖(CI 与本地开发使用)
# 安装: python -m pip install -r requirements-dev.txt
# ============================================================

# 单元测试框架
pytest>=8.0

# 轻量 linter(可选: ruff check agent)
ruff>=0.6
